? "%YAML 1.2"
platform:
  name: default_app
  description: description of the default_app

  environments:
    - INT
    - QA

  tiers:
    -

  #comments about available convention indicators
  nodenameconvention:
    - environment
    - '-'
    - component
    - '-'
    - instance
    - geo1


  pool:

    vmdefaults: &vmdefaults
      count:
      tier:
      image:
      compute:


  components:

    component:
      <<: *vmdefaults
      port: 80
